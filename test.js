//Only for Testing Purpose

/*var myfunction = () => {

    console.log('in the my Function');
};


setTimeout(myfunction, 1000);

var fs = require('fs')
var a=['new data','new data1','new data2'];
fs.appendFile('log.txt', a, function (err) {
  if (err) {
    // append failed
  } else {
    // done
  }
})  

*/